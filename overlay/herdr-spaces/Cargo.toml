[package]
name = "herdr-spaces"
version = "0.1.0"
edition = "2021"
description = "codex-style spaces (name + emoji + folders) for the herdr fork"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-width = "0.2"
